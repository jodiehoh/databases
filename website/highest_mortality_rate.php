<head>
   <title>Highest Mortality Rate</title>
</head>
<body>
<link rel="stylesheet" href="assets/css/main.css" />
<?php
include 'open.php';

//Override the PHP configuration file to display all errors
//This is useful during development but generally disabled before release
//ini_set('error_reporting', E_ALL);
//ini_set('display_errors', true);

if (isset($_POST['top'])) {
    $top = $_POST['top'];
}
if (isset($_POST['continent'])) {
    $continent = $_POST['continent'];
}
if (isset($_POST['year'])) {
    $year = $_POST['year'];
}

echo "<h2 style=\"text-align:center\">Highest Mortality Rate</h2>";

//Determine if any input was actually collected
if (empty($top) or empty($continent) or empty($year)) {
   echo "empty <br><br>";

} else {

   echo "<h3 style=\"text-align:center\"> Top ".$top." countries from ".$continent." in ".$year."</h3></br>";

   echo "<br><br>";
   //Prepare a statement that we can later execute. The ?'s are placeholders for
   //parameters whose values we will set before we run the query.
   if ($stmt = $conn->prepare("CALL HighestMortalityRate(?, ?, ?)")) {

      //Attach the ? in prepared statements to variables (even if those variables
      //don't hold the values we want yet).  First parameter is a list of types of
      //the variables that follow: 's' means string, 'i' means integer, 'd' means
      //double. E.g., for a statment with 3 ?'s, where middle parameter is an integer
      //and the other two are strings, the first argument included should be "sis".
      $stmt->bind_param("isi", $top, $continent, $year);

      //Run the actual query
      if ($stmt->execute()) {

         //Store result set generated by the prepared statement
         $result = $stmt->get_result();

         if ($result->num_rows == 0) {

            //Result contains no rows at all
            echo "No info.";

         } else {
              $rows = array();
              echo "<table border =\"2px solid black\">";
              echo "<tr><td>Country</td><td>Mortality Rate</td></tr>";
	      while ($row = $result->fetch_row()) {
		$value = $row[1] * 100;
                echo "<tr>";
                echo "<td>".$row[0]."</td>";
                echo "<td>".$value."%</td>";
                echo "</tr>";
	         } 
              echo "</table>";

              
              
          }   

         //We are done with the result set returned above, so free it
         $result->free_result();
      
      } else {

         //Call to execute failed, e.g. because server is no longer reachable,
   //or because supplied values are of the wrong type
         echo "Execute failed.<br>";
      }

      //Close down the prepared statement
      $stmt->close();

   } else {

       //A problem occurred when preparing the statement; check for syntax errors
       //and misspelled attribute names in the statement string.
      echo "Prepare failed.<br>";
      $error = $conn->errno . ' ' . $conn->error;
      echo $error; 
   }

}

//Close the connection created in open.php
$conn->close();
?>

<script>
</script>

<script src="https://canvasjs.com/assets/script/jquery-1.11.1.min.js"></script>
<script src="assets/js/canvasjs.min.js"></script>
</body>
